# ~/.zshenv — read by EVERY zsh (managed by chezmoi: home/dot_zshenv.tmpl).
# All the content lives in one file so .zprofile can source it a second time;
# see the header of ~/.config/zsh/env.zsh for why.
[ -r "$HOME/.config/zsh/env.zsh" ] && . "$HOME/.config/zsh/env.zsh"
