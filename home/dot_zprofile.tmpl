# ~/.zprofile — login shells (managed by chezmoi: home/dot_zprofile.tmpl).
# Second pass over the environment, after /etc/zprofile's path_helper has had
# its say — see ~/.config/zsh/env.zsh.
[ -r "$HOME/.config/zsh/env.zsh" ] && . "$HOME/.config/zsh/env.zsh"
